{
  "name": "bgm",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "turbo run build",
    "build:web": "corepack pnpm --filter @bgm/db db:generate && corepack pnpm --filter @bgm/web build",
    "build:api": "corepack pnpm --filter @bgm/db db:generate && corepack pnpm --filter @bgm/api build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "test:smoke": "node scripts/smoke-test.mjs",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "clean": "turbo run clean",
    "start:api": "corepack pnpm --filter @bgm/api start",
    "crawl:intents": "tsx scripts/crawl-intents.ts",
    "crawl:intents:it": "tsx scripts/crawl-intents.ts --department=IT",
    "crawl:intents:sales": "tsx scripts/crawl-intents.ts --department=SALES",
    "enrich:companies": "tsx scripts/enrich-companies.ts",
    "enrich:orgchart": "tsx scripts/enrich-orgchart.ts",
    "enrich:dept-phones": "tsx scripts/enrich-department-phones.ts"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "prettier": "^3.5.3",
    "@types/node": "^22.15.2",
    "tsx": "^4.21.0",
    "playwright": "^1.58.2",
    "next": "^15.3.1",
    "@prisma/client": "^6.6.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "msgpackr-extract",
      "prisma",
      "unrs-resolver"
    ]
  }
}
